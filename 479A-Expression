import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
        int c = sc.nextInt();
        int ans1 = a + b + c;
        int ans2 = a * b * c;
        int ans3 = a + (b * c);
        int ans4 = (a + b) * c;
        int ans5 = a * (b + c);
        int ans6 = (a * b) + c;
        int maxVal = Math.max(ans1, Math.max(ans2, Math.max(ans3, Math.max(ans4, Math.max(ans5, ans6)))));
        
        System.out.println(maxVal);
    }
}
